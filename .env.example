# VT Eating - Production Environment Template
# Copy to .env and fill in all values before running docker-compose.prod.yml
# NEVER commit .env to git (it's in .gitignore)

# Database - POSTGRES_PASSWORD is used by docker-compose for both postgres + DATABASE_URL
POSTGRES_PASSWORD=change-me-use-strong-password

# Auth
JWT_SECRET=change-me-run-openssl-rand-base64-32
ADMIN_EMAILS=yourname@vt.edu

# OTP emails (optional - leave empty to log codes to console)
# RESEND_API_KEY=
# FROM_EMAIL=VT Eating <noreply@yourdomain.com>

# Stripe - use LIVE keys for production
# Get keys from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_live_xxxxx
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_xxxxx
# Add webhook in Stripe Dashboard pointing to https://your-domain.com/api/webhooks/stripe
STRIPE_WEBHOOK_SECRET=whsec_xxxxx
